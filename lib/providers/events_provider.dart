
import 'package:flutter/material.dart';
import '../models/user_model.dart';

// class EventsProvider extends ChangeNotifier{

//   UserEvents? events;

//   void addEvents(List<DateTime> dates){
//     dates.forEach((date) => events?.optedDates.add(date));
//     dates.forEach((date) => events?.notOptedDates.add(date));
//     dates.forEach((date) => events?.unSignedDates.add(date));
//     notifyListeners();
//   }

//   UserEvents? get getEvents => events;
// }